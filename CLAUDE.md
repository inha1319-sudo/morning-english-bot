# 영어 학습 자동화 봇 프로젝트

## 📋 프로젝트 개요
매일 아침 영어 회화 주제를 받고, 저녁에 피드백을 기록하고, 매주 실수 패턴을 분석하는 3단계 자동화 시스템.

GitHub Actions 기반이므로 **컴퓨터가 꺼져있어도 항상 실행됨**.

---

## 🤖 봇 3개

### 1️⃣ **아침 봇** (`morning_english_bot.py`)
- **실행 시간**: 매일 오전 7:00 KST (UTC 22:00)
- **역할**: 
  - 오늘의 영어 회화 주제 1개 선택 (지난 7일과 중복 방지)
  - 관련 영어 표현 3개 제시 (한국어 의미 포함, 스포일러 태그로 숨김)
  - 대화 파트너 이름 1개 제시
  - 텔레그램으로 발송
- **데이터**: TOPICS_DATA (10개 주제), EXPRESSIONS_DATA (30+ 표현), NAMES_DATA (25개 이름)
- **외부 의존**: `ChatGPT지시문.md`, `history.json`

### 2️⃣ **저녁 봇** (`evening_check.py`)
- **실행 시간**: 매일 오후 7:00 KST (UTC 10:00)
- **역할**:
  - 텔레그램에서 사용자가 보낸 피드백 메시지 수신
  - 형식: `내가 쓴 문장: ... / 고쳐진 문장: ... / 실수의 종류: ...` (또는 줄바꿈 형식)
  - 피드백 분석 및 친근한 메시지로 응답
  - `실수장부.md`에 기록 (날짜|원문|수정문|실수종류)
  - 같은 실수 종류의 이번주 누적 개수 표시
- **외부 의존**: `읽음기록.json` (중복 처리 방지)

### 3️⃣ **주간 봇** (`weekly_summary.py`)
- **실행 시간**: 매주 일요일 오후 7:00 KST (UTC 일요일 10:00)
- **역할**:
  - 지난 7일간의 `실수장부.md` 데이터 분석
  - 대화 횟수, 자주 틀린 실수 종류 TOP 3 계산
  - 가장 많이 틀린 실수 종류를 다음주 학습 포커스로 결정
  - 포커스 관련 주제 5개 선택 (`ERROR_TYPE_TOPICS` 매핑)
  - `복습목록.md` 자동 생성 (덮어쓰기)
  - 텔레그램으로 주간 통계 발송

---

## 📁 파일 설명

### 🐍 Python 봇 파일
| 파일 | 역할 |
|------|------|
| `morning_english_bot.py` | 아침 주제 + 표현 생성/발송 |
| `evening_check.py` | 저녁 피드백 분석/기록 |
| `weekly_summary.py` | 주간 통계/복습목록 생성 |

### 🔐 설정 파일
| 파일 | 역할 | 주의 |
|------|------|------|
| `.env` | `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` | 로컬 테스트용, `.gitignore`로 제외됨 |
| `requirements.txt` | Python 의존성 (requests만 필요) | |
| `.github/workflows/` | 3개 GitHub Actions 워크플로우 YAML | GitHub Secrets에서 토큰 읽음 |

### 📊 데이터 파일 - 봇이 관리 (수동 편집 금지)
| 파일 | 생성자 | 역할 | 형식 |
|------|--------|------|------|
| `history.json` | 아침 봇 | 마지막 7개 주제명 저장 | JSON 배열 |
| `읽음기록.json` | 저녁 봇 | 처리한 메시지 ID 저장 | JSON 객체 |

### 📝 데이터 파일 - 사용자가 관리
| 파일 | 누가 만드나 | 역할 | 형식 | 중요한 점 |
|------|-----------|------|------|----------|
| **`실수장부.md`** | 저녁 봇 (자동 추가) | 모든 오류 누적 기록 | `날짜 \| 내가 쓴 문장 \| 고친 문장 \| 실수의 종류` | **영구 보존** (삭제 금지) |
| **`복습목록.md`** | 주간 봇 (매주 덮어쓰기) | 다음주 학습 계획 | `# 복습목록\n**다음 주 약점:** [에러타입]\n**학습 주제:**\n1. 주제1\n...` | **매주 초기화됨** (이전 내용 삭제) |

#### 🔑 실수장부 vs 복습목록
- **실수장부.md**: 누적 기록 (계속 쌓임) → 역사/분석용
- **복습목록.md**: 실행 계획 (매주 새로 씀) → 다음주 할일용

### 📖 참고 파일
| 파일 | 역할 |
|------|------|
| `ChatGPT지시문.md` | 아침 봇이 사용할 시스템 프롬프트 템플릿 |
| `표현정리.md` | (있으면) 표현 레퍼런스 |

---

## ⏰ 실행 일정

```
매일 아침 7:00 AM      → 아침 봇: 오늘의 주제 + 표현
       오후 6:00 PM    → 저녁 봇: 피드백 분석
매주 일요일 오후 6:00 PM → 주간 봇: 통계 + 다음주 계획 (복습목록.md 생성)
```

**시간대 변환:**
- KST (한국 시간) = UTC + 9시간
- 아침 7:00 KST = UTC 22:00 (전날) ← `cron: '0 22 * * *'`
- 저녁 6:00 KST = UTC 9:00 ← `cron: '0 9 * * *'`
- 일요일 저녁 6:00 KST = UTC 일요일 9:00 ← `cron: '0 9 * * 0'`

---

## ⚙️ 작업 시 지켜야 할 것

### ✅ 안전한 수정
- **주제나 표현 추가**: `morning_english_bot.py`의 `TOPICS_DATA`, `EXPRESSIONS_DATA` 수정 가능
- **에러 타입 매핑 추가**: `weekly_summary.py`의 `ERROR_TYPE_TOPICS` 수정 가능
- **피드백 형식 변경**: `evening_check.py`의 정규식 수정 필요 (사용자 형식에 맞춰)

### ❌ 금지 사항
- **절대 삭제 금지**:
  - `실수장부.md` (누적 데이터, 원본 기록)
  - `history.json` (아침 봇 필요)
  - `읽음기록.json` (저녁 봇 필요)
- **수동으로 커밋하지 말 것**:
  - `.github/workflows/` 파일들의 자동 커밋 처리 (`복습목록.md` 자동 커밋)
- **.env 커밋 금지**:
  - 토큰이 노출될 수 있음 (이미 `.gitignore`로 제외됨)
  - GitHub Secrets에만 저장

### 🔧 디버깅 시
1. **로컬 테스트**: `.env` 파일 준비 후 `python [스크립트명].py` 실행
2. **GitHub Actions 실행 확인**: https://github.com/inha1319-sudo/morning-english-bot/actions
3. **텔레그램 메시지 확인**: 각 실행 시간 전후로 채팅 확인
4. **에러 로그**: GitHub Actions 워크플로우 상세 페이지에서 확인

### 📋 코딩 규칙
- **파일 인코딩**: 모든 파이썬 코드에서 파일을 열 때 반드시 `encoding="utf-8"` 붙이기 (읽을 때도 쓸 때도 예외 없음)
  ```python
  with open("파일명.md", "r", encoding="utf-8") as f:
  with open("파일명.json", "w", encoding="utf-8") as f:
  ```
- **모든 .py 파일 맨 위에 추가**:
  ```python
  import sys
  sys.stdout.reconfigure(encoding="utf-8")
  ```
- **실수장부.md 수정**: 새 내용을 맨 끝에만 추가. 파일 전체를 읽어서 다시 쓰지 않기
- **파일 에러 대처**: 파일을 읽다가 실패하면 지우지 말고 이름만 바꾼 뒤 새로 시작
  ```python
  # 예: error_log.md → error_log_backup_20260901.md
  ```
- **윈도우 예약 작업**: `schtasks` 대신 PowerShell의 `Register-ScheduledTask` 사용
  - 한글 폴더명이 있으면 `schtasks`는 경로를 `?`로 바꿔 저장하면서 `SUCCESS` 표시 (함정!)

---

## 🚀 다음 단계 (향후 개선)

- [ ] 월간 통계 추가 (매월말 통계 리포트)
- [ ] 표현 난이도별 분류 (초급/중급/고급)
- [ ] 에러 타입별 학습 자료 추천
- [ ] 음성 피드백 지원 (텔레그램 음성 메시지)

---

**마지막 업데이트**: 2026-09-01
**담당자**: Claude (AI Assistant)
